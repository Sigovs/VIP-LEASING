# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files (can opt-in for committing if needed)
.env*

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

# Photography the client did NOT supply stays out of version control. This repo
# is public, and /ilusso and /inventory are still another dealership's shoot —
# they remain on disk so the site runs locally, and they go the day real
# inventory photography arrives.
#
# /public/site and /public/showcase are NOT ignored any more: both now hold the
# client's own frames, so a fresh clone has to be able to build the hero, the
# bands and the Lookbook.
/public/ilusso/
/public/inventory/

# The hero master. 140MB — over GitHub's own 100MB per-file limit, so it cannot
# be pushed even by accident, and it does not need to be: the page loads
# video_resize.mp4 (2.3MB, silent, faststart), which IS tracked. Same reasoning
# as source-images/ — the master stays on the machine that made it.
/public/video/video.mp4
/public/video/video_test_mian.png
